name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 监听 main 分支的变动
  workflow_dispatch:    # 允许你在网页上手动点按钮触发

# 设置权限
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 18 # 或者你本地用的 node 版本
          cache: 'npm'

      - name: Install dependencies 📦
        run: npm install

      - name: Build 🔧
        run: npm run build # 这里会生成 dist 文件夹

      - name: Upload artifact 📤
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist # ⚠️注意：如果你的打包输出目录不是dist，请修改这里

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4